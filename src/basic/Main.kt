package basic

fun main() {

    // 1. Entry point for all Kotlin language basics
    // Demonstrates variable declarations, types, and mutability (val vs var)
    Variable()
    // Demonstrates different function types: default args, overloading, etc.
    Function()
    // Demonstrates lambda expressions, higher-order functions, and syntax
    Lambda()
    // Demonstrates inline, noinline, crossinline, and null safety behaviors
    LambdaBehavior()


}